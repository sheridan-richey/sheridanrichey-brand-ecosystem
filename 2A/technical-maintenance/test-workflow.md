# Test workflow (reference)

**Canonical standards:** [.cursor/rules/testing-deployment.mdc](../../.cursor/rules/testing-deployment.mdc), [AGENTS.md](../../AGENTS.md), and [website/README.md](../../website/README.md).

Run tests from the repo root with `npm run test:e2e`, or from `website/` with `npm run test:e2e`.

This file is a **pointer** only. Update the rule file when E2E, CI, or deployment-validation policy changes.
