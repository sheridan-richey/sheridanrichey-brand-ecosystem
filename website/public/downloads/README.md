# Public downloads

Place the lead magnet file here:

- `prompt-architects-toolkit.pdf` — referenced by `/downloads/prompt-architects-toolkit`

`.gitignore` allows `*.pdf` in this folder only. Add the real PDF when ready, then commit.
